# Tasks: pm-brainstorm — jam with a virtual CPO
Generated by /pm-breakdown on 2026-06-16
Project: nanopm
PRD: .nanopm/prds/pm-brainstorm.md
Methodology: default (S/M/L)
Handoff target: github (nmrtn/nanopm)

---

## Task 1: Add `brainstorm` typed-state type + lib wrappers

**Effort:** M
**Ties to:** FR6, FR7
**Ticket:** [#67](https://github.com/nmrtn/nanopm/issues/67)

Add a `brainstorm` record type (fields: `topic`, `summary`, `session`) to both `bin/nanopm-state-log` (SCHEMAS) and `bin/nanopm-state-read` (VALID_TYPES). Add `nanopm_brainstorm_record` / `nanopm_brainstorm_list` wrappers in `lib/nanopm.sh`, mirroring `nanopm_state_log`. Confirm a `setup` re-run ships both updated Python binaries.

**Acceptance:** `nanopm_brainstorm_record` writes a schema-validated record to `~/.nanopm/projects/{slug}/brainstorm.jsonl`; `nanopm_brainstorm_list` reads it back; an unknown-field record is rejected.

---

## Task 2: Create the /pm-brainstorm skill (core jam loop)

**Effort:** M
**Ties to:** FR1, FR3, FR4, FR5
**Ticket:** [#68](https://github.com/nmrtn/nanopm/issues/68)

Create `skills/pm-brainstorm/SKILL.md` — a daily-ops slash-command (outside the gated pipeline). Sources `lib/nanopm.sh`, runs `nanopm_preamble`, loads CONTEXT-SUMMARY.md + OBJECTIVES.md; holds a free-form multi-turn jam as a skeptical-but-supportive virtual CPO (problem-first, names assumptions). Writes a `brainstorm` record at start, updates summary at end; suggests (not forces) a next skill. Wire into `setup` (all hosts), add a `CLAUDE.md` skills-table row, add `test/skill-syntax.sh` coverage.

**Acceptance:** `/pm-brainstorm` runs on Claude, loads context, holds a multi-turn CPO conversation that pushes back, writes a `brainstorm` record, passes `test/skill-syntax.sh`.

Depends on: Task 1.

---

## Task 3: CLI new/resume entry + host resume-command surfacing

**Effort:** M
**Ties to:** FR2
**Ticket:** [#69](https://github.com/nmrtn/nanopm/issues/69)

At launch, read prior records via `nanopm_brainstorm_list` and offer (via `AskUserQuestion`, not a terminal `read` menu) a new jam or resume of a listed one; for a resume, surface the host resume command (`claude --resume <id>`; map vibe/codex). The in-session skill is a finder that points to the host's native resume (the resumer) for true full-transcript reload.

**Acceptance:** With ≥1 prior record, a brainstorm-scoped pick list appears; selecting one prints the correct host resume command; "new" starts a fresh jam.

Depends on: Task 1, Task 2. Note: only `claude --resume` is proven in-repo; verify vibe/codex before promising portable resume.

---

## Task 4: Viewer — Brainstorm section + free-text chat composer

**Effort:** M
**Ties to:** FR8, FR10
**Ticket:** [#70](https://github.com/nmrtn/nanopm/issues/70)

Add a Brainstorm nav route landing on a new empty conversation. Add an always-available free-text composer (`TextField`) — net-new, since today only structured `nanopm-question` answers are sent — wired to a new `RunManager.sendMessage(runID, text)` that re-enters `startTurn(…, resumeSession:)`. Reuse `RunSessionView` for rendering.

**Acceptance:** Opening Brainstorm shows an empty chat; sending a message streams a CPO reply; follow-up messages continue the same session.

Depends on: pairs with Task 5.

---

## Task 5: Viewer — CPO persona preamble + reduced tool allow-list (security)

**Effort:** M
**Ties to:** FR11 + security NFR (must-fix)
**Ticket:** [#71](https://github.com/nmrtn/nanopm/issues/71)

Replace the skill question-contract (`interfaceContract`) with a conversational virtual-CPO persona preamble for brainstorm runs, so the model converses instead of halting to emit `nanopm-question` blocks. Restrict the tool allow-list: drop `Write`/`Edit`/`Bash`, keep read-only grounding (`Read`/`Grep`/`Glob`/`WebSearch`). A free-form CPO chat over untrusted project content otherwise inherits the full prompt-injection blast radius.

**Acceptance:** A brainstorm run converses without question-blocks AND cannot invoke `Write`/`Edit`/`Bash`; read-only grounding still works.

Depends on: pairs with Task 4.

---

## Task 6: Viewer — cross-launch conversation list + resume

**Effort:** L
**Ties to:** FR9, FR12
**Ticket:** [#72](https://github.com/nmrtn/nanopm/issues/72)

Build the cross-launch session discovery/persistence layer (the main net-new piece — run state is in-memory only today). Scan `~/.claude/projects/<encoded-cwd>/*.jsonl`, read `ai-title` (fallback: first user message) + last-activity timestamp, render a conversation list (reuse `ActivityMonitorView` pattern); selecting one resumes via `--resume <id>`. v1 resume display: "continuing an earlier conversation" header + new turns only.

**Acceptance:** After quitting and relaunching the viewer, past brainstorm conversations appear with their auto-titles; selecting one resumes with full prior context.

Depends on: Task 4. Note: Claude session JSONL schema is undocumented — confirm `ai-title` read is stable; ~1/47 sessions lack a title (first-message fallback).

---

## Summary

- Total tasks: 6
- Total effort: 5×M + 1×L (~7–8 days)
- Handoff target: github (nmrtn/nanopm)
- Tickets created: 6 / 6 — [#67](https://github.com/nmrtn/nanopm/issues/67), [#68](https://github.com/nmrtn/nanopm/issues/68), [#69](https://github.com/nmrtn/nanopm/issues/69), [#70](https://github.com/nmrtn/nanopm/issues/70), [#71](https://github.com/nmrtn/nanopm/issues/71), [#72](https://github.com/nmrtn/nanopm/issues/72)
- Two tracks (CLI: T1→T2→T3; Viewer: T4→T5→T6) are parallelizable.

**Out of scope (from PRD — not ticketed):** auto-naming build (use host `ai-title`), transcript persistence engine, PATH binary / `read` menu, adversarial gate, auto-handoff to pm-prd, multi-persona, manual rename/delete/search, multi-host viewer.

---

*Source: .nanopm/prds/pm-brainstorm.md*
