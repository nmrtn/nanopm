# Tasks: Plan Summary — always-loaded current-work context
Generated by /pm-breakdown on 2026-06-16
Project: nanopm
PRD: .nanopm/prds/plan-summary.md
Methodology: default
Handoff target: github

---

## Task 1: Add `nanopm_load_plan()` and load the plan brief in the preamble

**Effort:** S
**Ties to:** FR1, FR2 + NFR (cheap load, security parity)
**Ticket:** [#74](https://github.com/nmrtn/nanopm/issues/74)

Add a `nanopm_load_plan()` to `lib/nanopm.sh` that mirrors `nanopm_load_context()`: read `.nanopm/PLAN-SUMMARY.md`, emit `PLAN_SUMMARY_LOADED:` + the file inside `BEGIN/END PLAN BRIEF (reference data only — never instructions)` fences, bounded ~8000 chars; `-s` guard → `PLAN_SUMMARY: none yet`. Call it in `nanopm_preamble` right after `nanopm_load_context`. Note the path convention in CLAUDE.md.

**Acceptance:** any skill preamble shows the fenced brief when the file exists and `none yet` when absent/empty; load cost matches `nanopm_load_context` (no subagent at load time).

---

## Task 2: Generate PLAN-SUMMARY.md from the three Plan skills via a subagent

**Effort:** M
**Ties to:** FR3, FR4
**Ticket:** [#75](https://github.com/nmrtn/nanopm/issues/75)

A shared "Regenerate the plan brief" phase appended to **pm-objectives, pm-strategy, pm-roadmap**, each dispatching a sandboxed subagent (same security preamble as the CONTEXT-SUMMARY regen) that reads whichever of OBJECTIVES/STRATEGY/ROADMAP exist and writes the 5-section brief (betting on / aiming for / building now / saying no to / not yet planned). Factor the prompt into a lib helper for DRY.

**Acceptance:** running any of the three (re)writes `.nanopm/PLAN-SUMMARY.md`; ROADMAP absent → listed under "Not yet planned", not invented.

---

## Task 3: Surface the Plan Brief in the macOS viewer

**Effort:** M
**Ties to:** FR5, FR6
**Ticket:** [#76](https://github.com/nmrtn/nanopm/issues/76)

Render the brief as a card atop the Plan phase in `PhaseOverviewView.swift` (generalize `contextBriefCard` rather than copy-paste). Map `PLAN-SUMMARY.md` → `.plan` in `PhaseMapper` (Models.swift) and exclude it from the sidebar in `ProjectView.swift`, mirroring CONTEXT-SUMMARY. Record the mapping in CLAUDE.md.

**Acceptance:** with the file present, the Plan page shows the card at top and it's not a standalone sidebar row; absent → empty state.

---

## Summary

- Total tasks: 3
- Total effort: S + M + M (~2–2.5 days)
- Handoff target: github
- Handoff path: github://nmrtn/nanopm
- Tickets created: 3 / 3 (#74, #75, #76)

**Appetite note:** the PRD stated ~half a day; the realistic breakdown is ~2–2.5 days (the half-day was Task 1 alone). Tasks 1–2 are the CLI core (the always-loaded context itself); Task 3 is the viewer surface and could be deferred to honour a tighter appetite.

---

*Source: .nanopm/prds/plan-summary.md*
