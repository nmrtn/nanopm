# Tasks: Close the Opportunity → Solutions → PRD loop in the viewer
Generated by /pm-breakdown on 2026-06-28
Project: nanopm
PRD: .nanopm/wiki/docs/prds/opportunity-solutions-launch.md
Methodology: default
Handoff target: github

---

## Build Plan

Build Plan — 5 tasks across 3 waves

Wave 0 (foundation, build first, then merge): Task 1, Task 2
Wave 1 (parallel after Wave 0): Task 3, Task 4
Wave 2 (after Wave 1): Task 5

Max parallel width: 2. Critical path: 3 waves. Foundation ≈ 25% of effort.

The two foundation tasks touch disjoint files (RunManager/SkillCatalog vs SolutionsUI) so they run in parallel. Wave 1 keeps the heavy `OpportunitiesUI.swift` work (Task 3) and the new `CompareCardView.swift` (Task 4) in separate files so they don't collide. Task 5 is the single-file integration that finishes `OpportunityDetailView` and must follow both.

---

## Task 1: Per-opportunity run tracking key

**Effort:** M
**Wave:** 0
**Depends on:** none
**Ties to:** R2
**Ticket:** [#155](https://github.com/nmrtn/nanopm/issues/155)

Make `RunManager` `isActive`/`latestRun` key on an opportunity-scoped tracking path instead of the shared `wiki/entities/solutions/INDEX.md` the `pm-solutions` catalog entry uses (`SkillCatalog.swift:211`, `RunManager.swift:210`). Pass a synthetic per-opportunity `trackingPath` (e.g. `solutions:<opp-slug>`) at the call site, or add a launch param.

**Acceptance:** Two different opportunities can have concurrent solutions runs and `isActive`/`latestRun` answer per-opportunity (one strip does not light the other).

---

## Task 2: Extend Solution parser — riskiest-assumption + cheapest-test

**Effort:** S
**Wave:** 0
**Depends on:** none
**Ties to:** R5
**Ticket:** [#156](https://github.com/nmrtn/nanopm/issues/156)

Extend the `Solution` model/parser (`SolutionsUI.swift:10`, parse at `:37`/`:60`) to expose riskiest-assumption and cheapest-test, using the same one-line-under-heading technique used for `summary`.

**Acceptance:** `Solution` exposes non-empty riskiest-assumption and cheapest-test when present; absent headings degrade to empty without crashing.

---

## Task 3: Launch button + popover + run lifecycle on OpportunityDetailView

**Effort:** L
**Wave:** 1
**Depends on:** 1
**Ties to:** R1, R3
**Ticket:** [#157](https://github.com/nmrtn/nanopm/issues/157)

On `OpportunityDetailView`'s header (`OpportunitiesUI.swift:321`), add a "Brainstorm solutions" `ActionButton` opening a pre-launch popover (reuse `SkillRunButton.swift:58`) with an optional comment field and a one-click Run that works empty. On Run, call `RunManager.launch(_:in:userContext:)` resolving the command to `/pm-solutions <opportunity-slug>` with the comment as `userContext`. Render the lifecycle inline: running strip → "Answer…" routing to `QuestionForm` (`RunSessionView.swift:155`) → success banner → inline error. No auto-navigation.

**Acceptance:** Clicking launches `/pm-solutions` for the open opportunity; the strip reflects running → answer → done/error inline; browsing unaffected if the CLI is missing.

**GUI test:** 1. Open an opportunity detail. 2. Click "Brainstorm solutions", leave comment empty, click Run. 3. Assert an inline running strip appears with elapsed time and the page stays put. 4. On completion assert a success banner replaces the strip. _(Auto-run if a GUI harness/computer-use is available, else manual QA.)_

---

## Task 4: CompareCardView component + "Spec this →"

**Effort:** M
**Wave:** 1
**Depends on:** 2
**Ties to:** R4 (component), R6
**Ticket:** [#158](https://github.com/nmrtn/nanopm/issues/158)

Create `CompareCardView.swift` (NEW) — a standalone view taking `[Solution]` and rendering 3-up compare cards (pitch, lens·appetite, impact, riskiest assumption, cheapest test; reuse `SolutionsUI.swift` tints `:125`). Each card has `[Open]` and a "Spec this →" button invoking a passed-in launch closure. Does not edit OpportunitiesUI.swift.

**Acceptance:** Given ≥3 solutions, aligned cards render with all five axes; `[Open]` and "Spec this →" call their closures with the right solution.

**GUI test:** 1. Render CompareCardView with 3 sample solutions. 2. Assert three columns each show pitch + appetite + impact + riskiest + cheapest. 3. Click "Spec this →" on column 2 and assert the closure fires with solution #2's slug. _(Manual QA / harness.)_

---

## Task 5: Embed compare section + "N solutions" badge

**Effort:** M
**Wave:** 2
**Depends on:** 3, 4
**Ties to:** R4 (integration), R7
**Ticket:** [#159](https://github.com/nmrtn/nanopm/issues/159)

Append a Solutions-candidates section to `OpportunityDetailView` (`OpportunitiesUI.swift:371`): query solutions whose parent is this opportunity (`SolutionsUI.swift:98`) and host the CompareCardView from Task 4. Wire each "Spec this →" to `RunManager.launch` for `/pm-prd <solution-slug>` — launching the PRD is the act of choosing, no frontmatter write. Tie the success banner from Task 3 to this section. Add a "N solutions" count badge to the detail header and the overview table row (`:264`). Degrade to "no solutions yet" before the first run.

**Acceptance:** After a solutions run the detail shows the candidate cards; "Spec this →" launches `/pm-prd` for that solution; a "N solutions" badge shows on detail and table row; no write to `.nanopm/` from the viewer.

**GUI test:** 1. Open an opportunity with ≥3 solutions on disk. 2. Assert a "Solutions candidates" section with that many cards renders below the body and the header shows an "N solutions" badge. 3. Click "Spec this →". 4. Assert a `/pm-prd` run starts (running strip) scoped to that solution. _(Auto-run if harness/computer-use available, else manual QA.)_

---

## Summary

- Total tasks: 5
- Total effort: M + S + L + M + M (≈ 6 days)
- Waves: 3 (Wave 0 foundation + 2 parallel waves; max parallel width 2)
- Handoff target: github
- Handoff path: github://nmrtn/nanopm
- Tickets created: 5 / 5 — #155, #156, #157, #158, #159

---

*Source: .nanopm/wiki/docs/prds/opportunity-solutions-launch.md*
