# Tasks: Feedback ingestion and learning loop
Generated by /pm-breakdown on 2026-06-30
Project: nanopm
PRD: .nanopm/wiki/docs/prds/feedback-ingestion-and-learning-loop.md
Methodology: default
Handoff target: github (nmrtn/nanopm)

---

## Build Plan

7 tasks across 3 waves.

- **Wave 0** (foundation — build and merge first): T1
- **Wave 1** (parallel after Wave 0): T2, T3, T4, T7
- **Wave 2** (parallel after Wave 1): T5, T6

Max parallel width: 4. Critical path: 3 waves (T1 → T2 → T6/T5).

⚠ Plan note (advisory, from the collision check): T5 owns **all** `pm-interview` removal in `setup` (both the `_SKILL_LIST` entry and the help-text echo). Do not run `test/plugin-manifest.sh` to validate T4's directory deletion until T5's registry sync lands, or `setup` will transiently reference a deleted skill.

---

## Task 1: Raw substrate — archive helper + manifest schema

**Effort:** L
**Wave:** 0
**Depends on:** none
**Ties to:** R2, R7
**Ticket:** [#163](https://github.com/nmrtn/nanopm/issues/163)

Create the raw-archive + bidirectional-link substrate. `nanopm_archive_raw` (content-hash id, idempotent) and `nanopm_raw_manifest` in `lib/nanopm.sh`; raw-hash dedup in `bin/nanopm-ingest-agent`; document `raw/<type>/<id>` + `<id>.manifest.jsonl` in `NANOPM-WIKI.md`.

**Acceptance:** re-archiving the same source yields the same id and no duplicate; the manifest records the opportunity↔claim↔raw-line link; skill citations resolve to the archived file.

---

## Task 2: /pm-add-feedback skill — the ingestion loop

**Effort:** L
**Wave:** 1
**Depends on:** T1
**Ties to:** R1, R3, R4, R5, R6, R7, R10
**Ticket:** [#164](https://github.com/nmrtn/nanopm/issues/164)

New `pm-add-feedback/SKILL.md` (single file). Intake (path/paste/connector) → archive (T1) → discovery extraction (Mom-Test) → Pass 1 ground existing opps (reuse `nanopm-ingest-agent`) → Pass 2 new opps strategy-aware → batched confirm + `⚠ low-confidence` → write manifest links → emit structured run-summary. Critical path.

**Acceptance:** pasting a feedback grounds ≥1 existing opportunity with a resolvable verbatim, or proposes a new opportunity; ends with a machine-readable run-summary.

---

## Task 3: Viewer — Raw feedback browser

**Effort:** M
**Wave:** 1
**Depends on:** T1
**Ties to:** R11
**Ticket:** [#165](https://github.com/nmrtn/nanopm/issues/165)

`PhaseMapper` carve-out (`Models.swift`) for `raw/interviews` + `raw/feedback`; new `RawFeedbackUI.swift` (mirrors `OpportunitiesUI`); sidebar + detail wiring (`ProjectView.swift`, `SkillCatalog.swift`). Surfaces manifest metadata from T1.

**Acceptance:** archived raw sources are browsable read-only with their content.

**GUI test:** 1. Open the viewer on a project with files under `.nanopm/raw/feedback`. 2. Open the "Raw feedback" section. 3. Assert the sources are listed and clicking one shows its content in the detail pane.

---

## Task 4: /pm-discovery interview-prep + retire pm-interview

**Effort:** M
**Wave:** 1
**Depends on:** none
**Ties to:** R8
**Ticket:** [#166](https://github.com/nmrtn/nanopm/issues/166)

Add an interview-prep mode to `pm-discovery/SKILL.md`; delete the `pm-interview/` directory. Does NOT edit `setup`/`plugin.json` (registry owned by T5).

**Acceptance:** `/pm-discovery` produces an interview-prep guide; nothing but the registry references `pm-interview` (handled in T5).

---

## Task 7: Tests — raw-archive idempotency + manifest link

**Effort:** M
**Wave:** 1
**Depends on:** T1
**Ties to:** R2, R7 (success-criteria test row)
**Ticket:** [#167](https://github.com/nmrtn/nanopm/issues/167)

New `test/` entry covering `nanopm_archive_raw` idempotency and the bidirectional manifest link, under the existing tier-1 harness.

**Acceptance:** fails if archiving stops being idempotent or the manifest link breaks.

---

## Task 5: Registry + version lockstep

**Effort:** S
**Wave:** 2
**Depends on:** T2, T4
**Ties to:** R9
**Ticket:** [#168](https://github.com/nmrtn/nanopm/issues/168)

Remove **every** `pm-interview` reference in `setup` (the `_SKILL_LIST` entry AND the help echo); add `pm-add-feedback`. Update `.claude-plugin/plugin.json` skills + bump `version`; bump `VERSION`. In lockstep per `test/plugin-manifest.sh`.

**Acceptance:** `bash test/plugin-manifest.sh` passes; `setup` has no `pm-interview` reference and lists `pm-add-feedback`.

---

## Task 6: Viewer — + Add feedback + digest

**Effort:** M
**Wave:** 2
**Depends on:** T2, T3
**Ties to:** R12
**Ticket:** [#169](https://github.com/nmrtn/nanopm/issues/169)

"+ Add feedback" paste affordance in `ActivityMonitorView.swift` launching `/pm-add-feedback` via `RunManager`; register the skill in `SkillCatalog.swift`; new `FeedbackDigestView` rendering the R10 structured run-summary (opps created/updated, provenance transitions, themes).

**Acceptance:** pasting a feedback in the viewer launches the run, streams it live, and shows a digest of what changed on completion.

**GUI test:** 1. Click "+ Add feedback", paste a sample feedback, run. 2. Assert the Activity Monitor shows the live run. 3. On completion, assert a digest lists ≥1 opportunity touched/created.

---

## Summary

- Total tasks: 7
- Total effort: 2×L, 4×M, 1×S
- Waves: 3 (Wave 0 foundation + 2 parallel waves; max parallel width 4)
- Handoff target: github (nmrtn/nanopm)
- Tickets created: 7 / 7 — [#163](https://github.com/nmrtn/nanopm/issues/163), [#164](https://github.com/nmrtn/nanopm/issues/164), [#165](https://github.com/nmrtn/nanopm/issues/165), [#166](https://github.com/nmrtn/nanopm/issues/166), [#167](https://github.com/nmrtn/nanopm/issues/167), [#168](https://github.com/nmrtn/nanopm/issues/168), [#169](https://github.com/nmrtn/nanopm/issues/169)

---

*Source: .nanopm/wiki/docs/prds/feedback-ingestion-and-learning-loop.md*
