# Tasks: Subagent-powered context & review for pm-prd
Generated by /pm-breakdown on 2026-06-15
Project: nanopm
PRD: .nanopm/prds/pm-prd-subagent-context-and-review.md
Methodology: default (none set) — plain tasks, S/M/L effort
Handoff target: github (nmrtn/nanopm)

Two independent chains: **Phase 2** (Task 1 → 2 → 3) and **Phase 4b** (Task 4 → 5 → 6).

---

## Task 1: Add `nanopm_prd_retrieval_prompt` helper to lib

**Effort:** M
**Ties to:** FR2, FR6, The One UX Decision (Option B)
**Ticket:** [#54](https://github.com/nmrtn/nanopm/issues/54)

New helper `nanopm_prd_retrieval_prompt <doc> <feature> <intent>` in `lib/nanopm.sh` that prints a feature-keyed, per-doc retrieval-subagent prompt. Reuse the trust-boundary + bounded-digest (≤~200 words) + `.nanopm/{FILE}.md` pointer contract of `nanopm_retrieval_prompt` (lib:811), but key on the feature + each doc's specific extraction intent, and require the doc's structured flag in the output.

**Acceptance:** emits a correct prompt for each of the 5 docs (PERSONAS, DATA, PRODUCT, BUSINESS-MODEL, FEEDBACK) with the right intent + flag spec; `bash test/skill-syntax.sh` passes.

---

## Task 2: Rewrite pm-prd Phase 2 as a parallel retrieval fan-out

**Effort:** L
**Ties to:** FR1, FR2
**Depends on:** Task 1 (#54)
**Ticket:** [#55](https://github.com/nmrtn/nanopm/issues/55)

Replace the sequential per-doc `Read`/extract blocks in `pm-prd/SKILL.md` Phase 2 with: detect which of the 5 context docs exist → dispatch one retrieval subagent per present doc in a single concurrent turn → consume the digests. The main agent must not `Read` those docs raw. FEEDBACK-first → Dovetail fallback unchanged.

**Acceptance:** on a ≥2-doc run, the transcript shows a single concurrent fan-out and zero raw full-doc reads of those docs in the main agent; the PRD still draws on each doc's relevant slice.

---

## Task 3: Drive Phase 2 control-flow gates from the subagent flags

**Effort:** M
**Ties to:** FR3
**Depends on:** Task 2 (#55)
**Ticket:** [#56](https://github.com/nmrtn/nanopm/issues/56)

Wire the existing Phase 2 decisions to the returned flags: `FEATURE_SERVES: anti` → STOP and flag; `PRODUCT_COMPLETENESS: draft` → one-line non-blocking warning; cite only 🟢 DATA metrics. Subagents inform; the main agent halts/decides.

**Acceptance:** an anti-persona feature triggers the STOP; a draft PRODUCT.md yields the one-line warning; a 🔴 metric is never cited as a fact in the drafted PRD.

---

## Task 4: Add the review-lens prompt set to lib

**Effort:** M
**Ties to:** FR4, FR6
**Ticket:** [#57](https://github.com/nmrtn/nanopm/issues/57)

New helper(s) in `lib/nanopm.sh` emitting the advisory lens prompts — appetite/scope realism, success-criteria measurability, persona-fit, dependency/feasibility — each with the untrusted-input guard and the strict `LENS / VERDICT: PASS|CONCERN / NOTE:` 3-line contract. Falsifiability reviewer prompt unchanged.

**Acceptance:** each lens prompt emits; a dry-run lens returns exactly the 3-line contract; `bash test/skill-syntax.sh` passes.

---

## Task 5: Extend Phase 4b into a concurrent review panel + `## Reviewer notes`

**Effort:** L
**Ties to:** FR4, FR5
**Depends on:** Task 4 (#57)
**Ticket:** [#58](https://github.com/nmrtn/nanopm/issues/58)

In `pm-prd/SKILL.md` Phase 4b, dispatch the falsifiability reviewer (unchanged 4-line contract + `nanopm_state_log` gate) alongside the advisory lenses in one concurrent turn. Append each CONCERN under a new `## Reviewer notes` block, labelled by lens — without touching the falsifiability FAIL path or the state writes.

**Acceptance:** panel runs concurrently; falsifiability gate + state writes behave as today; a PRD seeded with a vague criterion + unscoped dependency yields ≥1 non-falsifiability CONCERN; new scenario in `test/adversarial.e2e.sh`.

---

## Task 6: Make panel gating build_mode-aware (solo-fast advisory / team-traditional blocking)

**Effort:** M
**Ties to:** FR5
**Depends on:** Task 5 (#58)
**Ticket:** [#59](https://github.com/nmrtn/nanopm/issues/59)

Reuse the 4b.2 `build_mode` read. `solo-fast`: CONCERN advisory, state writes proceed. `team-traditional`: CONCERN escalates to a hard block — Phase 4b stops, Phase 5 writes don't run until resolved/waived.

**Acceptance:** a solo-fast PRD with a CONCERN completes (`prd=ready`); the same PRD under team-traditional blocks until waived; both paths in `test/adversarial.e2e.sh`; context-threading e2e passes.

---

## Summary

- Total tasks: 6
- Total effort: 4×M + 2×L (~9 days)
- Handoff target: github
- Handoff path: github://nmrtn/nanopm
- Tickets created: 6 / 6 — [#54](https://github.com/nmrtn/nanopm/issues/54), [#55](https://github.com/nmrtn/nanopm/issues/55), [#56](https://github.com/nmrtn/nanopm/issues/56), [#57](https://github.com/nmrtn/nanopm/issues/57), [#58](https://github.com/nmrtn/nanopm/issues/58), [#59](https://github.com/nmrtn/nanopm/issues/59)

---

*Source: .nanopm/prds/pm-prd-subagent-context-and-review.md*
