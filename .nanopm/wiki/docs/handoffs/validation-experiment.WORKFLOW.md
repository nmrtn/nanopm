---
tracker:
  kind: linear
  api_key: $LINEAR_API_KEY
  project_slug: nanopm-validation
  active_states:
    - Todo
    - In Progress
  terminal_states:
    - Done
    - Cancelled
    - Canceled

polling:
  interval_ms: 30000

workspace:
  root: ~/.symphony/workspaces

agent:
  max_concurrent_agents: 3
  max_turns: 20

codex:
  command: codex app-server
  turn_timeout_ms: 3600000
  read_timeout_ms: 5000
  stall_timeout_ms: 300000
---

# Workflow: Validation Experiment

You are a Codex agent working on a ticket from a nanopm-generated PRD. nanopm produced this WORKFLOW.md to give you the full PM context.

## Context for this ticket

- **Issue:** `{{ issue.identifier }}: {{ issue.title }}`
- **Attempt:** {% if attempt %}{{ attempt }}{% else %}1 (first run){% endif %}
- **Source PRD:** `.nanopm/prds/validation-experiment.md` (read this first — it's the source of truth)
- **Project state directory:** `~/.nanopm/projects/nanopm/` (typed decisions live here)

## The bet behind this work

> Solo founders and small teams adopting agentic dev environments value an upstream PM tool that produces typed PM artifacts (bet, falsification, scope-outs) and lets them choose their delivery layer across 6 peer targets (Linear, GitHub, OpenSpec, gstack, Symphony, Human). Symphony's 2026-06-02 launch is the timing lever for a coordinated communication push, not the strategy itself. Success at day 14 post-launch: at least 10 GitHub stars AND at least one engagement from any of Symphony, OpenSpec, gstack, or Linear/PM communities.

## Falsification — how we'd know this PRD was wrong

> Fewer than 3 of the first 10 external solo founders (running on Claude Code, Vibe, or Codex) who complete a `/pm-run` invoke any second nanopm skill that emits a `memory-read` event reading from `~/.nanopm/projects/{slug}/decision.jsonl` within 14 days of their first `pm-run` completion timestamp.

## What to do

1. Read `.nanopm/prds/validation-experiment.md` carefully. The PRD is the source of truth. If anything here conflicts with the PRD, the PRD wins.
2. (Optional) Read `~/.nanopm/projects/nanopm/decision.jsonl` for additional context on prior PM decisions (typed bet, scope-out, target).
3. Implement only this ticket's scope. Each ticket is independently shippable.
4. Open a PR. PR body must reference: (a) the PRD path, (b) this ticket's `{{ issue.identifier }}`, (c) the success criterion from below.
5. Move the issue state to `Review` (or the equivalent handoff state in this Linear workflow) when done.

## What to NOT do

- **Paid promotion** (HN front-page bump, Reddit ads, sponsored Discord posts) — organic only.
- **Analytics dashboard / UI** — read raw `timeline.jsonl` + `decision.jsonl` directly.
- **Reformulating the README** — locked by STRATEGY.md `not-polished-marketing` scope-out.
- **Adding new skills / connectors** — locked by STRATEGY.md `not-49-skill-catalog` and `not-new-connectors`.

## Acceptance — how to know this ticket is done

- First-time pipeline completions by external users: ≥10 within 14 days of poll launch.
- Second-skill invocations that read prior decision state: ≥3 of the first 10 completers within 14 days.
- Self-reported "this changed what I'd build next": ≥1 explicit confirmation by day 14.
- What will be different in commits: new `bin/nanopm-memory-read-ping` (or equivalent in `lib/nanopm.sh`); `event=memory-read` lines in test fixtures; results write-up under `.nanopm/intel/VALIDATION-{date}.md`.

## Multi-host note

This workflow runs under Symphony (Codex App Server). The source PRD and typed-state files were produced by nanopm running on Claude Code, Mistral Vibe, or OpenAI Codex — the artifacts are host-agnostic. If you spot something in the PRD that uses a Claude-specific construct (e.g., `AskUserQuestion`), translate it to your environment's equivalent (Codex handles questions conversationally).

---

*This WORKFLOW.md was generated by nanopm /pm-breakdown --target=symphony on 2026-06-03. The PRD at `.nanopm/prds/validation-experiment.md` is the canonical specification. Symphony spec compatibility: v1 (Linear tracker, repo-owned WORKFLOW.md).*
