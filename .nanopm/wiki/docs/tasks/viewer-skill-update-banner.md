# Tasks: Viewer skill-update banner & one-click update
Generated by /pm-breakdown on 2026-06-15
Project: nanopm
PRD: .nanopm/prds/viewer-skill-update-banner.md
Methodology: default
Handoff target: github (nmrtn/nanopm)

---

## Task 1: Add install-provenance marker to `setup`

**Effort:** S
**Ties to:** FR7
**Ticket:** [#32](https://github.com/nmrtn/nanopm/issues/32)

Where `setup` writes `~/.nanopm/VERSION` (~setup:390), also write `~/.nanopm/install-source`: the repo path (`$_REPO_ROOT`) when `_SOURCE=local`, else `remote`. One-line addition; values already exist.

**Acceptance:** after a local `bash setup` the file holds the repo path; after a curl install it holds `remote`.

---

## Task 2: Version-check service (local vs remote VERSION)

**Effort:** M
**Ties to:** FR1, NFR
**Ticket:** [#33](https://github.com/nmrtn/nanopm/issues/33)

Swift service: on launch reads `~/.nanopm/VERSION`, fetches remote `VERSION`, compares with the same semver rules as `nanopm_update_check` (24h cache, fail-silent offline), async so it never blocks launch.

**Acceptance:** with a stale local VERSION it reports update-available + both versions; offline it returns no-update with no error or launch delay.

---

## Task 3: Update banner UI (dismissible, both versions)

**Effort:** M
**Ties to:** FR2, FR6
**Ticket:** [#34](https://github.com/nmrtn/nanopm/issues/34)

Passive, dismissible banner shown only when remote > local, naming both versions, Claude-Code style. Dismiss hides for the session. Depends on Task 2.

**Acceptance:** banner appears only when an update exists, shows both versions, dismiss removes it until next launch.

---

## Task 4: "Update now" action — shell-out + silent spinner/result

**Effort:** M
**Ties to:** FR3, FR4, FR5
**Ticket:** [#35](https://github.com/nmrtn/nanopm/issues/35)

Button shells out to `curl …/setup | bash` via ShellRunner; spinner during run; read exit code. Success → re-read VERSION, clear banner, confirm. Failure → plain error + terminal fallback, banner stays. No console shown. Depends on Tasks 3 & 5.

**Acceptance:** click updates the pack (`~/.nanopm/VERSION` = remote), banner clears, confirmation shown; simulated failure shows error + fallback and keeps banner.

---

## Task 5: Maintainer guard (dev-install detection)

**Effort:** S
**Ties to:** FR8
**Ticket:** [#36](https://github.com/nmrtn/nanopm/issues/36)

Before upgrading, read `~/.nanopm/install-source`. Clone path → block + warn naming the path, no curl. `remote`/absent → proceed. Depends on Task 1.

**Acceptance:** with `install-source` = a repo path, click shows the warning and runs no curl; with `remote`/no file it proceeds normally.

---

## Summary

- Total tasks: 5
- Total effort: 3×M + 2×S
- Handoff target: github (nmrtn/nanopm)
- Tickets created: 5 / 5
- Build order: T1 → T5, T2 → T3 → T4

---

*Source: .nanopm/prds/viewer-skill-update-banner.md*
