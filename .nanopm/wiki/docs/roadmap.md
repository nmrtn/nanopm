---
type: doc
skill: pm-roadmap
provenance: user-stated
generated: 2026-06-25
sources: []
---

# Product Roadmap
Generated by /pm-roadmap on 2026-06-03
Project: nanopm
Strategy: validate that typed-state memory is the pull — 3 of 10 first-time `pm-run` completers fire a second skill reading `decision.jsonl` within 14 days.

---

## NOW (Next 4-8 weeks)

*What we're building right now. Committed, sequenced, assigned. Exactly one item per the strategy: every other candidate is deferred behind one of the four scope-out decisions.*

| Item | Outcome | Owner | Effort | Ties to |
|------|---------|-------|--------|---------|
| Validation experiment | At least 3 of the first 10 external solo founders running `/pm-run` on Claude Code, Vibe, or Codex invoke a second nanopm skill that reads from `~/.nanopm/projects/{slug}/decision.jsonl` within 14 days of their first run | Builder | L (2-3 weeks elapsed; ~3 eng-days) | Obj 1 / KR1-KR3 — Strategy bet `validate-typed-memory-pulls-return` |

**Sub-tasks within the NOW item (not separate roadmap items):**
1. Wire a return-ping mechanism that logs when a skill reads `decision.jsonl` for an existing project (single-line append to `timeline.jsonl` with event=`memory-read`). ~1 day.
2. Post the April location poll across the 4 channels named in STRATEGY.md (Claude Code Discord, r/ClaudeAI, Show HN, Vibe community). ~0.5 day.
3. Follow up at +24h, +72h, +7d, +14d with the first 10 completers. ~0.5 day spread across 14 days.
4. Daily check on `decision.jsonl` reads across non-`nanopm` slugs. Automated. 0 days incremental.

**Capacity check:** 1 item, ~3 eng-days + 14 elapsed days of wall-clock. Solo capacity assumed at ~1 eng-week/month. Fit.

**Scope-out enforcement:** the four scope-out decisions from STRATEGY.md (`not-49-skill-catalog`, `not-polished-marketing`, `not-new-connectors`, `not-tagline-vs-mycelium`) BLOCK any temptation to add a second NOW item this cycle.

---

## NEXT (1-3 months out)

*Directionally committed — but conditional on the NOW outcome. If the validation experiment fails, the strategy changes and these items don't apply.*

- **If validation passes (≥3 of 10):** Convert validated users into steady install rate. Target: 3 new external installs per week sustained for 4 weeks via Claude Code marketplace listing + weekly Discord/HN presence. → ties to Obj 1 distribution sub-goal.
- **If validation passes:** Wire `/pm-retro` to read `decision.jsonl` and compare committed `target` decisions against actual shipped state (closing the planning loop). → ties to Obj 2 (honest about what shipped).
- **If validation fails (<3 of 10):** Decide between library-mode pivot (nanopm exposed as a function library other agents call) vs park, within 7 days of the result. This is a NEXT decision, not a NEXT build item. → ties to Strategy's "kill or pivot" action.

---

## LATER (3+ months / when we get there)

*Good ideas that aren't the priority now. Each item must include a re-open condition or it gets cut.*

- **Plugin-ify the install (vs current setup-script copy model)** — revisit when ≥5 external users report install friction OR when 2+ host vendors (Claude / Vibe / Codex) ship a plugin-marketplace mechanic.
- **Migrate remaining skills (`pm-objectives`, `pm-discovery`, `pm-user-feedback`, `pm-retro`) to write typed-state decisions** (currently only `pm-strategy` / `pm-audit` / `pm-roadmap` / `pm-prd` / `pm-breakdown` do) — revisit when ≥3 external users complete a pipeline (i.e., after NOW resolves favorably).
- **Real Codex e2e test suite** — revisit when first Codex user reports a failure OR when we want to advertise Codex support strongly (currently soft / unverified).
- **`/pm-retro` enrichment with typed decision diffs** (compare `target` decisions to actual shipped behavior via git + analytics) — revisit when 2+ external users complete a pipeline AND ship something.

---

## Explicitly NOT on the roadmap

*Pulled from STRATEGY.md scope-out decisions. Each lists the specific condition that would change the decision.*

- **Not adding a 16th–49th skill.** Cap at 17. — Reconsider only if ≥3 external users complete a pipeline AND request the same missing skill by name.
- **Not rewriting the README in polished marketing voice** (counter-positioning to gstack's "Software Factory"). — Reconsider only if external users explicitly report the README is unreadable.
- **Not adding a 16th connector.** — Reconsider only on the second unprompted feature request for the same connector.
- **Not competing on tagline copywriting** (vs mycelium's "AI has made building cheap"). — Reconsider only if the README converts <2% of GitHub visitors to install attempts over 60 measured days.
- **Not building distribution infrastructure** (marketplace listing automation, install counters, install funnel optimization) **until validation passes.** This is the most important "no" — if you start building distribution before knowing whether anyone wants it, you're building marketing for a product that hasn't proven its use case.

---

*Sources: STRATEGY.md (2026-06-03 — typed `bet` decision + 4 typed `scope-out` decisions), OBJECTIVES.md (Objective 1 with KR1-KR3 demand validation framing), AUDIT.md (section 3 unrun-poll gap, section 4 external-user question), INTEL-2026-06-03.md (competitive movement context). No connector data pulled — solo project, no Linear / Notion / GitHub backlog.*
