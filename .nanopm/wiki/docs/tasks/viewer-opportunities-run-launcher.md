# Tasks: Opportunities page — launch a /pm-opportunities run from the viewer
Generated by /pm-breakdown on 2026-06-18
Project: nanopm
PRD: .nanopm/prds/viewer-opportunities-run-launcher.md
Methodology: default (none set)
Handoff target: github (nmrtn/nanopm)

---

## Task 1: Build the reusable dedup subagent (`pm-opportunity-dedup`)

**Effort:** L · **Ties to:** FR8
**Ticket:** [#90](https://github.com/nmrtn/nanopm/issues/90)

Standalone subagent invoked by `/pm-opportunities`, built reusable (not inlined in `generate`). Input: candidate(s) (title + problem text + optional quote + provenance) + the current `.nanopm/opportunities/` DB. Output per candidate: `new` | `duplicate-of:<slug>` | `merge-into:<slug>` + confidence (0–10) + one-line rationale. Contract stable enough for a future transcript→opportunity extractor to reuse unchanged.

**Acceptance:** a near-identical candidate → `merge-into:<slug>` with rationale; a novel one → `new`; contract documented for reuse.

---

## Task 2: Teach `/pm-opportunities` the `add:`/`generate:` hint contract

**Effort:** M · **Ties to:** FR4 (routing), FR5
**Ticket:** [#91](https://github.com/nmrtn/nanopm/issues/91)

Parse a structured hint from run context (`add: …` / `generate: …`) and route directly, falling back to auto-detect when absent. Update the opportunities `headlessArgs` in `SkillCatalog.swift` so both ends agree on the format.

**Acceptance:** `add:<text>` → add; `generate:<N> [for theme X]` → generate; no hint → unchanged auto-detect; terminal use backward-compatible.

---

## Task 3: Implement the additive `generate` mode in `/pm-opportunities`

**Effort:** L · **Ties to:** FR4 (generate) · **Depends on:** Task 1 (#90), Task 2 (#91)
**Ticket:** [#92](https://github.com/nmrtn/nanopm/issues/92)

Draft N opportunities (global or one theme, reusing bootstrap's per-theme fan-out inside the single skill run), route each candidate through the dedup agent, write survivors as `nano-hypothesis` + `LOG.md`, never overwrite. Batch policy: auto-merge above the high-confidence bar, else create-new tagged `related-to:<slug>`; print a merge/skip summary.

**Acceptance:** `generate: 3 for theme "Trust & evidence"` → ≤3 new nano-hypothesis files (fewer if merged), each logged, existing files untouched, summary printed.

---

## Task 4: Route Mode A (add) through dedup with the interactive merge prompt

**Effort:** M · **Ties to:** FR2 (skill side), FR8 (interactive policy) · **Depends on:** Task 1 (#90), Task 2 (#91)
**Ticket:** [#93](https://github.com/nmrtn/nanopm/issues/93)

In the add path, run the typed problem through dedup; on a high-confidence duplicate, ask merge-into-X / keep-as-new / cancel via the run's question contract; else write new.

**Acceptance:** a near-duplicate surfaces the choice; merge appends to the existing file + `LOG.md`; a novel problem writes new with no prompt.

---

## Task 5: Add the `+ Run ▾` launch menu to the Opportunities header

**Effort:** L · **Ties to:** FR1, FR6, FR7
**Ticket:** [#94](https://github.com/nmrtn/nanopm/issues/94)

Add the menu to `OpportunitiesOverviewView`'s header in `OpportunitiesUI.swift`. Empty-DB (no `SCHEMA.md`) → collapse to a single "Bootstrap opportunities" action. Disable while a run is active (`RunManager.isActive`); on `completionTick` trigger `store.refresh()`.

**Acceptance:** menu renders; no `SCHEMA.md` → only Bootstrap; disabled during an active run; list auto-refreshes after completion.

---

## Task 6: Mode A sheet — "Add one from text"

**Effort:** M · **Ties to:** FR2 (viewer side) · **Depends on:** Task 2 (#91), Task 5 (#94)
**Ticket:** [#95](https://github.com/nmrtn/nanopm/issues/95)

Menu item opens a sheet with a multiline `TextField`; Run calls `RunManager.launch(..., userContext: "add: <text>")`; Cancel dismisses. Reuse the `SkillRunButton` popover pattern.

**Acceptance:** problem + Run launches with the `add:` hint and typed text; Cancel closes with no run.

---

## Task 7: Mode B submenu — "Generate N more" (Global / By theme)

**Effort:** M · **Ties to:** FR3 · **Depends on:** Task 2 (#91), Task 5 (#94)
**Ticket:** [#96](https://github.com/nmrtn/nanopm/issues/96)

Submenu with "Global" + one entry per live theme (derived from existing `theme:` frontmatter); selecting launches `RunManager.launch(..., userContext: "generate: <N> [for theme <theme>]")` with default N.

**Acceptance:** submenu lists current DB themes; selecting Global or a theme launches generate with the correct hint.

---

## Summary

- Total tasks: 7
- Total effort: 3×L + 4×M (≈ 11.5 days solo, splittable across two)
- Handoff target: github
- Handoff path: github://nmrtn/nanopm
- Tickets created: 7 / 7 (#90–#96)

**Build seam:** Mode A path = Tasks 1, 2, 4, 5, 6. Mode B adds Tasks 3, 7. If the appetite tightens, ship Mode A first and fast-follow B (the PRD's stated fallback).

**Open before building (from the PRD):** pin the high-confidence dedup threshold for auto-merge (start strict), and the default value of N — both owned by Guillaume, due 2026-06-25.

---

*Source: .nanopm/prds/viewer-opportunities-run-launcher.md*
