# Tasks: Viewer action UI harmonization (Run / Refresh / Reasoning)
Generated by /pm-breakdown on 2026-06-15
Project: nanopm
PRD: .nanopm/prds/viewer-action-ui-harmonization.md
Methodology: default
Handoff target: github

---

## Task 1: Build shared ActionButton component

**Effort:** M
**Ties to:** FR1, FR6
**Ticket:** [#61](https://github.com/nmrtn/nanopm/issues/61)

Create one reusable SwiftUI view backing all harmonized actions: SF Symbol icon + text label, enabled/disabled state, optional busy/state variant (Run's "Running…"/"Answer…"), `.help()` passthrough, reusing existing `np*` colors. Prereq decision (Guillaume): Option A vs B — does it need a menu mode for Competitors?

**Acceptance:** Shared button view exists under `viewer/Sources/NanoPMViewer/`, renders icon + label with consistent styling, supports enabled/disabled + busy variant, forwards `.help()`.

---

## Task 2: Migrate Run on phase overview to ActionButton

**Effort:** M
**Ties to:** FR2
**Ticket:** [#62](https://github.com/nmrtn/nanopm/issues/62)

Replace the plain text Run button at `PhaseOverviewView.swift:184`, preserving the context popover, `runManager.launch(...)`, and the Run / Answer… / Running… states.

**Acceptance:** Run uses the shared button, opens the popover, launches via RunManager, shows three states, keeps its tooltip — no regression.

---

## Task 3: Migrate Refresh to ActionButton

**Effort:** S
**Ties to:** FR3
**Ticket:** [#63](https://github.com/nmrtn/nanopm/issues/63)

Replace the icon-only refresh at `ProjectView.swift:168-175` with an icon+label button (`arrow.clockwise` + "Refresh"), keeping `store.refresh()` and auto-refresh triggers.

**Acceptance:** Refresh uses the shared button with icon + label, still refreshes, auto-triggers fire, tooltip preserved.

---

## Task 4: Migrate Reasoning controls to ActionButton

**Effort:** M
**Ties to:** FR4
**Ticket:** [#64](https://github.com/nmrtn/nanopm/issues/64)

Unify the reasoning toggle across `ArtifactDetailView` (`ProjectView.swift:432-454`) and `CompetitorDetailView.swift:152-161` to the shared button (`brain` + "Reasoning"), consistent on both.

**Acceptance:** Reasoning triggered by the same treatment on both screens, pane/sidecar switching works, tooltips preserved.

---

## Task 5: Harmonize Competitors Run menu + separate-window reasoning button

**Effort:** M
**Ties to:** FR5
**Ticket:** [#65](https://github.com/nmrtn/nanopm/issues/65)

Bring the Competitors 3-option Run `Menu` (`CompetitorDetailView.swift:145-150`) and the `macwindow.on.rectangle` reasoning-window button (`ProjectView.swift:441-453`) into the same button family, reusing `np*` colors, without dropping functionality.

**Acceptance:** Both controls match the shared button family; all menu options + separate-window open still work; tooltips preserved.

---

## Summary

- Total tasks: 5
- Total effort: 4×M + 1×S
- Handoff target: github
- Handoff path: github://nmrtn/nanopm
- Tickets created: 5 / 5

---

*Source: .nanopm/prds/viewer-action-ui-harmonization.md*
